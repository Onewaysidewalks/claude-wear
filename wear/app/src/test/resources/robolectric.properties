# Roborazzi needs Robolectric to draw for real rather than no-op the Canvas.
graphicsMode=NATIVE

# Match compileSdk. Pinning it here rather than per-test keeps a golden from silently being
# re-rendered against a different platform's fonts and widget metrics.
sdk=34
